% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assess_stability.R
\name{stab}
\alias{stab}
\title{Stability across prompts or models (A step of CRAFT)}
\usage{
stab(
  predictions,
  gold,
  reliability_method = "cohen",
  reliability_level = "nominal",
  validity_metrics = c("f1_macro", "mcc", "balanced_accuracy")
)
}
\arguments{
\item{predictions}{A named list of prediction vectors, all of the
same length. Names appear as the \code{variant} column of the
returned table.}

\item{gold}{Gold-standard reference vector, same length as each
prediction.}

\item{reliability_method, reliability_level}{Passed to
\code{\link{reliab}}.}

\item{validity_metrics}{Passed to \code{\link{valid}}.}
}
\value{
A data frame with one row per variant.
}
\description{
Given a named list of predicted-label vectors (one per prompt or
model variant) plus a gold reference, compute reliability + validity
for each variant and stack the results into a tidy comparison table.
}
\examples{
\dontrun{
  stab(
    list("GPT-5" = labels$gpt5,
         "Gemini-3" = labels$gemini,
         "Llama-3.3" = labels$llama),
    gold = labels$human
  )
}

}
